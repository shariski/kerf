# Commits listed here are hidden from `git blame` (local + GitHub).
#
# GitHub reads this file automatically. For local `git blame` to respect it:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add commits that are purely mechanical (formatting, rename, lint
# auto-fix) — never real logic changes.

# chore: apply Biome formatter to entire codebase (2026-04-23)
14773cf42d7ee9318e53a422affd60a758752643
