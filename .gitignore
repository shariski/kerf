node_modules/
.DS_Store
Thumbs.db
.worktrees/
dist/
dist-ssr
*.local
.env
.env.*
!.env.example
.nitro/
.tanstack/
.wrangler
.output/
.vinxi
.idea/
__unconfig*
todos.json

# Local tooling artifacts (Serena MCP cache, Superpowers skill docs/state, Playwright MCP session files)
.serena/
# docs/superpowers/
.superpowers/
.playwright-mcp/

# The repo uses pnpm (see CLAUDE.md §B13). A stray package-lock.json from
# an accidental `npm install` would silently fight the pnpm-lock.yaml and
# produce a hybrid node_modules. Ignore so it never gets committed.
package-lock.json

# Playwright a11y test artifacts
test-results/
playwright-report/
audit-*.png
t2-*.png
t3-*.png
t4-*.png
t6-*.png
t7-*.png
logo-fix-verify.png

# Added by code-review-graph
.code-review-graph/
