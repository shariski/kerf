# Handoff prompt — continue ADR-003 implementation on laptop

> Paste everything below the `─────` line into a fresh Claude Code session in the kerf repo.

─────

I'm continuing ADR-003 ("Deliberate-Practice Architecture") implementation that another session of yours started. Eight of twenty tasks have shipped across two PRs; you're picking up at Task 9. Read this whole prompt before doing anything.

## Anchor

- **Repo**: `kerf` (this directory)
- **Plan (canonical, fully spec'd, ~3.6K LOC)**: `docs/superpowers/plans/2026-04-22-deliberate-practice-architecture.md`. Each task block in there has the full TDD steps, code blocks, test code, and commit message. Treat this as your bible.
- **ADR**: `docs/00-design-evolution.md` ADR-003 (canonical product intent)
- **Spec docs reflecting ADR-003**: `docs/01-product-spec.md` (v0.5), `docs/02-architecture.md` (v0.3), `docs/03-task-breakdown.md` (v0.3) — all already updated by Pass 1
- **Behavior rules**: `CLAUDE.md` Parts A + B. Part B has the project-specific rules including §B2 (phase + journey awareness), §B3 (no-verdict copy), §B6 (Phase A scope discipline), §B7 (no LLM content gen), §B8 (exhaustive domain unit tests), §B9 (no production SSH), §B10 (challenge and clarify), §B11 (README Status checkpointing)

## What's already shipped (Tasks 1–8)

**PR #48 (merged)** — Foundation slice:
- **Task 1**: nullable `finger_assignment` text column on `keyboard_profiles` (Drizzle migration `0003_wild_proudstar.sql`)
- **Task 2**: `JourneyCode = "conventional" | "columnar" | "unsure"` type at `src/domain/adaptive/journey.ts` + `toJourneyCode()` narrowing helper. `UserBaseline.journey: JourneyCode` field added; every existing `UserBaseline` literal got `journey: "conventional"` (the default for the more common transition path). `computeBaseline()` signature gained a `journey` parameter and threads it through both empirical and PHASE_BASELINES cold-start paths (the cold-start fix was a code-review follow-up).
- **Task 3**: Onboarding step 4 with the journey question per ADR-003 §2 exact wording. `JourneyQuestion` component extracted to `src/components/onboarding/`. `createKeyboardProfile` server-fn at `src/server/profile.ts` accepts and persists `fingerAssignment` after `toJourneyCode()` narrowing. Settings toggle is **not** in this PR (it's Task 11).

**PR #49 (status: ask user — was open at handoff time)** — Engine-core slice:
- **Task 4**: `weaknessScore.ts` extension. `JOURNEY_BONUSES` map + `isOffHomeRow` helper. Bonus only in transitioning phase, only on characters (not bigrams). Function signature unchanged — journey is sourced from `baseline.journey`.
- **Task 5**: `motionPatterns.ts` (new). `verticalColumnCandidates` (10), `innerColumnCandidates` (2), `thumbClusterCandidate` (1). Each scores by aggregate error rate / `baseline.meanErrorRate`. Module is journey-blind — journey weighting happens at Target Selection (Task 8). Docstring is honest that motion scores are **not** comparable with `computeWeaknessScore` output (the latter applies ALPHA/BETA/GAMMA/DELTA coefficients; motion does not).
- **Task 6**: `drillLibrary.ts` (schema + `isValidDrillEntry` + `lookupDrill`). `targetSelection.ts` was created here as a **type-only stub** (just `TargetType` and `SessionTarget` types) to break the circular dep with drillLibrary. Task 8 replaced the stub with the full implementation.
- **Task 7**: `drillLibraryData.ts` — 33 pre-authored entries (20 vertical-column + 8 inner-column + 5 thumb-cluster). Exercise strings are mechanically derived from the finger table; briefing copy uses stub markers like `"{V1 conventional}"` — Task 9 replaces those via `buildBriefing()`.
- **Task 8**: Full `targetSelection.ts`. `selectTarget(stats, baseline, phase, frequencyInLanguage)` returns the journey-weighted argmax `SessionTarget`. `TARGET_JOURNEY_WEIGHTS` encodes per-journey promotion (conventional promotes vertical-column 1.2× and demotes inner-column 0.6×; columnar inverts). Returned `SessionTarget.score` is the **weighted** product (engine score × journey weight) — matches the docstring + `session_targets.selection_score` DB column intent. Low-confidence stats → `diagnosticTarget()`. Hand-isolation and cross-hand-bigram are NOT engine-selected; they're drill-mode only.

**Smoke test for Tasks 4–8** (works locally on PR #49 branch):
```bash
./node_modules/.bin/tsx scripts/demo-target-selection.ts
```
Output shows the same weak-stats input getting picked as `vertical-column / left-index-inner` under conventional and `inner-column / inner-left` under columnar. The weighted score is identical (2.800) because the underlying error rate is the same — only the journey-promoted target type changes.

## Cumulative state on main (assuming PR #49 merged)

- DB has the `finger_assignment` column. New profiles persist a journey; pre-ADR-003 profiles are NULL until the one-time card lands (Task 11).
- Engine has full target-selection capability. **But nothing in the running app calls it yet.** `/practice` still uses `generateExercise` directly. The wiring lands in Task 17.
- `npm test` should report 714/714 passing (one pre-existing Playwright `tests/a11y.spec.ts` failure is unrelated and present on main).
- `npm run typecheck` clean.

## What's next: Tasks 9–10 (recommended slice for next PR)

**Task 9 — `briefingTemplates.ts`** (pure domain, ~10–15 min subagent work):
- New file `src/domain/adaptive/briefingTemplates.ts` + `.test.ts`
- Implements `buildBriefing(target, journey, phase) → { text, keys }`
- V1 (vertical-column) and V2 (inner-column) have journey-specific copy (different framing for conventional vs columnar)
- V3 (thumb-cluster), V4 (hand-isolation), V5 (cross-hand bigram), V6 (character/bigram), V7 (diagnostic) are journey-shared
- Every string self-checks against CLAUDE.md §B3 — no hype, no verdict, quietly affirming
- Plan section: lines roughly 1909–2178 of the plan file (extract via `sed -n '1909,2178p'`)

**Task 10 — `sessionGenerator.ts`** (domain integration, ~20–30 min):
- New file `src/domain/adaptive/sessionGenerator.ts` + `.test.ts`
- Wraps `selectTarget` + (`exerciseGenerator` for character/bigram targets OR `lookupDrill` for motion targets) + `buildBriefing`
- Returns `SessionOutput = { target, exercise, briefing, estimatedSeconds }`
- Drill mode: takes a `targetOverride` SessionTarget and skips `selectTarget`
- **Note**: existing `exerciseGenerator` shape (in this codebase) differs from the plan's example. Inspect `src/domain/adaptive/exerciseGenerator.ts` first to see how `weaknessScoreFor` is wired, then adapt the call shape in `sessionGenerator.ts`. The plan flags this in Task 10's "Note" paragraph.
- Plan section: lines roughly 2179–2435 of the plan file

After Tasks 9–10 land, Tasks 11–14 are the persistence slice (settings + capture card, `session_targets` table, `persistSession` extension, `sessionStore` accumulator). Then 15–18 are UI. Then 19–20 are integration.

## How to execute (the workflow that produced 8 successful tasks)

1. **Set up a worktree** before doing implementation work. The skill `superpowers:subagent-driven-development` requires it. Steps:
   ```bash
   git fetch --prune origin
   git pull --ff-only          # if on main
   git worktree add ../kerf-adr-003 -b feat/adr-003-engine-templates
   cd ../kerf-adr-003
   cp ../kerf/.env .env        # or recreate per .env.example
   npm install                 # ~1-2 min
   ```
2. **Invoke the skill**: `Skill superpowers:subagent-driven-development`. Read it. It defines the per-task loop: dispatch implementer → spec reviewer → code-quality reviewer → fix loop → mark complete.
3. **For each task**: extract the full task spec text from the plan (use `sed -n` with the line range), embed verbatim in the implementer prompt (the skill explicitly says "never make a subagent read the plan file — paste the full text"). Include scene-setting context: which task in the plan, what dependencies are already shipped, where the worktree is.

## Operational lessons learned in the prior session (save you ~30 min of trial and error)

1. **LSP diagnostics go stale after subagent file creation.** When a subagent creates a new module, the IDE's TypeScript indexer may report "Cannot find module './newModule'" for ~30 seconds. Trust `npm run typecheck` exit code over IDE diagnostics. Real state is the source of truth.

2. **rtk proxy intercepts `npx`.** `npx tsx scripts/foo.ts` fails with "Missing script: 'tsx'" because rtk routes it as `npm tsx`. Use `./node_modules/.bin/tsx` for direct binary invocation. `npx vitest` similarly fails — use `npm test -- <path>` instead.

3. **Model selection cheat sheet:**
   - **Haiku**: pure-domain TDD with full plan code blocks (Tasks 1, 2, 4, 5, 6, 7, 8 worked great with haiku). DB migrations. Doc-only fixes. Tiny code-review-driven fixes.
   - **Sonnet**: UI work in large existing files (Task 3 onboarding.tsx was 24K — sonnet handled it). Multi-file integration where pattern-matching matters. Anything where the plan provides a skeleton but not literal code.
   - **Opus**: not needed for any task in this plan if the spec is faithful.

4. **Two-stage review is genuinely worth it.** Five real defects were caught and fixed in-branch before PR:
   - Task 2: `computeBaseline` cold-start path discarded the journey parameter
   - Task 3: typecheck errors (TS2532) on `mock.calls[0][0]` access; aria-label on radios overrode implicit label and hid descriptions from screen readers
   - Task 5: docstring overclaimed score comparability between motion-pattern and character/bigram scores
   - Task 6: redundant `.length === 0` checks; lenient `.toContain` test that should have been `.toBe`
   - Task 8: `SessionTarget.score` returned unweighted candidate score instead of journey-weighted product (would have broken Task 10 + the `session_targets.selection_score` DB column)
   
   Spec review catches "did the code implement what's named in the spec." Code-quality review catches "does the code's behavior match what callers will expect, beyond the spec's literal text." Both are needed.

5. **Pre-existing Playwright failure** (`tests/a11y.spec.ts`) is unrelated to ADR-003 and present on main. Don't get distracted by it. Document it in test plans as "known failure."

6. **Per-task subagent invocation pattern that worked**:
   - Implementer prompt = full task text + scene-setting context + behavior format (Status: DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT) + commit message
   - Spec reviewer prompt = original spec + implementer's claim + verification commands + "DO NOT trust the report" instruction
   - Code reviewer prompt = use `superpowers:code-reviewer` subagent_type with template at `requesting-code-review/code-reviewer.md` — focus on architecture/quality, NOT spec compliance (already verified)
   - Fix-loop subagent = narrow, surgical, only the changed file(s), explicit commit message

7. **Branch + PR cadence:** small slices ship faster than big slices. Each PR has 2–5 tasks based on natural seams. PR titles: `feat(adr-003): <slice name> (Tasks X–Y)`. PR body uses HEREDOC with Summary + Test plan + References.

8. **Conventional commit format** (preserved across all 14+ commits in PRs #48 and #49):
   ```
   feat(adaptive): <one-line summary> (ADR-003 §X)
   
   <body explaining the why, not the what>
   
   Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
   ```
   
   Use `feat(scope):`, `fix(scope):`, `refactor(scope):`, `chore(scope):`, `docs(scope):` per the conventional-commit style. Commit messages are passed via HEREDOC in `git commit -m "$(cat <<'EOF' ... EOF)"`.

## Per-slice time estimates (calibrated from observed wall time)

| Slice | Tasks | Est. wall time |
|---|---|---|
| C (next) | 9, 10 | **45–60 min** |
| D | 11, 12, 13, 14 | 90–120 min |
| E | 15, 16, 17, 18 | 120–180 min |
| F | 19, 20 | 45–60 min |
| **Total remaining** | | **~5–7 hours** |

The ADR-003 §6 original estimate was 5–7 weeks of Claude Code work. Subagent-driven development collapses this significantly because (a) implementation runs without context-switching, (b) two-stage review catches issues before PR, (c) fix-loops are narrow and fast.

## Quality bar (don't autopilot past these)

- **CLAUDE.md §B3 no-verdict rule** is load-bearing for all UI copy. Every string in briefing templates (Task 9), `SessionBriefing` component (Task 15), `TargetRibbon` (Task 16), post-session blocks (Task 17) needs to self-check. Banned: pass/fail badges, celebratory language, `!!`, "amazing/crushing/nailed/incredible". The platform surfaces numbers; the user judges themselves.
- **CLAUDE.md §B4 a11y** matters more than first-instinct suggests. Task 3 had an `aria-label` regression that hid descriptions from screen readers — caught by code review. Future radio/SVG/keyboard-shortcut work needs attention.
- **CLAUDE.md §B6 Phase A scope** — when something tempts you to add "just a little more" (e.g., difficulty progression, drill CMS, multi-target sessions), stop. Those are explicitly Phase B per ADR-003 §6.
- **CLAUDE.md §B10 challenge and clarify** — when the plan or instructions feel ambiguous, ask before dispatching a subagent. The cost of one clarifying question is much less than the cost of an off-target implementation.

## First action

Don't start implementing yet. Instead:

1. Confirm you've internalized this prompt (one-line summary back to me)
2. **Verify state**:
   - `git -C . status` → clean?
   - `git log --oneline -3` → does main include both PR #48 and PR #49? If PR #49 isn't merged yet, ask the user before continuing.
   - `npm run typecheck` and `npm test` — do they pass?
3. **Confirm scope**: am I starting on Tasks 9–10 as recommended, or does the user want a different slice?
4. **Set up worktree** per the skill (`superpowers:using-git-worktrees` is required by `superpowers:subagent-driven-development`)
5. Then dispatch Task 9 implementer per the workflow.

If anything in this prompt contradicts the plan or the ADR, the plan + ADR win — they're the canonical source. This prompt just speeds up the first 30 minutes.

─────

End of handoff prompt.
